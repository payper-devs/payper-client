/**
 * Shared Layer
 *
 * Contains reusable code that doesn't depend on business logic:
 * - ui: UI kit components (buttons, inputs, etc.)
 * - lib: Utility functions
 * - api: API client and request handlers
 * - config: Application configuration
 * - types: Shared TypeScript types
 */

export * from "./lib";
